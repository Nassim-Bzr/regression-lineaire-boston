# regression-lineaire-boston git init git add main.py train.csv git commit -m first commit git branch -M main git remote add origin git@github.com:Nassim-Bzr/regression-lineaire-boston.git git push -u origin main
# regression-lineaire-boston git init git add main.py train.csv git commit -m first commit git branch -M main git remote add origin git@github.com:Nassim-Bzr/regression-lineaire-boston.git git push -u origin main
# regression-lineaire-boston
